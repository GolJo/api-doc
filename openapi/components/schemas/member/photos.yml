Schema:
  type: array
  items:
    type: object
    properties:
      id:
        type: integer
        format: int64
        example: 1
      path:
        type: string
        example: ファイル名
      url:
        type: string
        example: ファイルパス
